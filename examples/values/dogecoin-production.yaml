dogecoinConf:
  rpcuser: user
  testnet: 1
  rpcallowip:
    # Option 1: Allow all (NOT recommended for production)
    - 0.0.0.0/0

rpcPassword: ""  # This will be automatically stored as a Kubernetes secret

service:
  port: 44556             # 44556 for testnet, 22556 for mainnet
  rpcPort: 44555          # 44555 for testnet, 22555 for mainnet

# Storage
storage:
  size: 50Gi              # Should be > 200Gi for mainnet
  storageClassName: gp3-immediate      # Storage class with immediate binding 

# Enable ingress
ingress:
  enabled: true
  className: "nginx"
  annotations:
    nginx.ingress.kubernetes.io/backend-protocol: "TCP"
  hosts:
    - host: dogecoin.testnet.dogeos.com
      paths:
        - path: /
          pathType: Prefix
  tls:
    - secretName: dogecoin-tls
      hosts:
        - dogecoin.testnet.dogeos.com
 
