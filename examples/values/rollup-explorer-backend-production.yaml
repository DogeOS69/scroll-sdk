controller:
  replicas: 2

ingress:
  main:
    ingressClassName: "nginx"
    hosts:
      - host: ""
        paths:
          - path: /
            pathType: Prefix

resources:
  requests:
    memory: "200Mi"
    cpu: "50m"
  limits:
    memory: "2Gi"
    cpu: "2000m"
