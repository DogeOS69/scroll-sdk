# PLACEHOLDER — replace with signer-policy.env from the bridge operator's V2
# bundle. Also copy protocol_context.json and, for production, the aggregate
# verifying key into ./policy/. The current dogeos-core signer cannot start
# before it receives canonical protocol context; generate the public descriptor
# with `scrollsdk signer init` first and deploy only after bundle handoff.
#
# An Issue #843 testnet recovery bundle may additionally contain
# ATTESTATION_SIGNER_PRE_TSUKI_DIRECT_SIGN_MAX_END_BATCH_HEIGHT. Never add or
# change that deployment-wide pin by hand; WP, TSO, and this signer must use the
# same reviewed Tsuki-boundary height.
