# PLACEHOLDER — replace this file with signer-policy.env from the bridge
# operator's post-genesis policy bundle, and copy the bundle's
# verifier-registry.toml + source-set.toml into ./policy/, then
# `docker compose up -d` again.
#
# Until then the signer runs in its pre-genesis shape: healthy, able to
# report its public key on /health, but not yet bound to a bridge.
