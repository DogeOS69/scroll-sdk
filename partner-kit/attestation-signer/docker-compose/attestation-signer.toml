# PLACEHOLDER partner-owned dogeos-core attestation-signer V2 policy.
# Replace this file with signer-<id>/attestation-signer.toml generated by
# `scrollsdk signer init`. The bridge operator's policy bundle never overwrites
# it because RPC trust domains and rotation targets belong to this operator.
