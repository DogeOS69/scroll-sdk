{
  "$schema": "https://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "controller": {
      "type": "object",
      "properties": {
        "type": {
          "const": "statefulset",
          "description": "eth-da-submitter has a fixed StatefulSet workload model."
        },
        "replicas": {
          "type": "integer",
          "minimum": 0,
          "maximum": 1,
          "description": "eth-da-submitter is singleton-only and does not support multiple replicas."
        },
        "strategy": {
          "const": "RollingUpdate",
          "description": "eth-da-submitter uses the chart-owned RollingUpdate strategy."
        }
      }
    }
  }
}
