{
  "$schema": "https://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "controller": {
      "type": "object",
      "properties": {
        "type": {
          "const": "statefulset",
          "description": "withdrawal-processor has a fixed StatefulSet workload model."
        },
        "replicas": {
          "type": "integer",
          "minimum": 0,
          "maximum": 1,
          "description": "withdrawal-processor is singleton-only and does not support multiple replicas."
        },
        "strategy": {
          "const": "RollingUpdate",
          "description": "withdrawal-processor uses the chart-owned RollingUpdate strategy."
        }
      }
    }
  }
}
