{{- if .Values.protocolContext.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  labels:
    {{- include "scroll-common.labels" $ | nindent 4 }}
  name: {{ .Values.protocolContext.name | default "protocol-context-config" }}
data:
  protocol_context.json: |
{{ .Values.protocolContext.data | indent 4 }}
{{- end }}
