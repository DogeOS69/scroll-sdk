global:
  nameOverride: &app_name cubesigner-signer
  fullnameOverride: *app_name

controller:
  type: statefulset
  replicas: 1
  strategy: RollingUpdate

image:
  repository: dogeos69/cubesigner-signer
  pullPolicy: IfNotPresent
  tag: v0.3.0-beta.2

service:
  main:
    enabled: true
    ports:
      http:
        enabled: true
        port: 3000

defaultProbes: &default_probes
  enabled: true
  custom: true
  spec:
    httpGet:
      path: "/health"
      port: http

probes:
  liveness:
    <<: *default_probes
  readiness:
    <<: *default_probes
  startup:
    <<: *default_probes
    spec:
      httpGet:
        path: "/health"
        port: http
      initialDelaySeconds: 10
      periodSeconds: 5
      failureThreshold: 12

# Direct environment variables for CubeSigner Signer configuration
env:
  - name: DOGEOS_CUBESIGNER_SIGNER_PORT
    value: "3000"
  - name: DOGEOS_CUBESIGNER_SIGNER_NETWORK
    value: "testnet"
  - name: DOGEOS_CUBESIGNER_SIGNER_TSO_URL
    value: "http://dogeos-tso:3000"
  - name: DOGEOS_CUBESIGNER_SIGNER_PROTOCOL_CONTEXT_JSON
    value: "/app/protocol_context.json"
  - name: DOGEOS_CUBESIGNER_SIGNER_SIGNATURE_DELAY
    value: "1"
  - name: DOGEOS_CUBESIGNER_SIGNER_POLL_INTERVAL
    value: "5000"
  - name: DOGEOS_CUBESIGNER_SIGNER_CS_KEY_ID
    value: ""
  - name: DOGEOS_CUBESIGNER_SIGNER_CS_SESSION_PATH
    value: "/etc/cubesigner/session.json"
  - name: DOGEOS_CUBESIGNER_SIGNER_BODY_LIMIT
    value: "5mb"
persistence:
  # Mount session file from a Secret
  session:
    name: cubesigner-session-secret-vol
    enabled: true
    type: secret
    secretName: cubesigner-default-session-secret    # Default name, override per instance
    mountPath: /etc/cubesigner
    readOnly: true
  protocol-context:
    name: protocol-context-config
    enabled: true
    type: configMap
    mountPath: /app/protocol_context.json
    subPath: protocol_context.json
    readOnly: true

# Resource configuration
resources:
  requests:
    memory: "128Mi"
    cpu: "100m"
  limits:
    memory: "512Mi"
    cpu: "500m"

# Monitoring configuration
serviceMonitor:
  main:
    enabled: true
    labels:
      release: scroll-sdk
    serviceName: '{{ include "scroll.common.lib.chart.names.fullname" $ }}'
    endpoints:
      - port: http
        interval: 10s
        scrapeTimeout: 5s
