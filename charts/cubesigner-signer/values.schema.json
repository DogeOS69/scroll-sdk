{
  "$schema": "https://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "controller": {
      "type": "object",
      "properties": {
        "type": {
          "const": "statefulset",
          "description": "cubesigner-signer has a fixed StatefulSet workload model."
        },
        "replicas": {
          "type": "integer",
          "minimum": 0,
          "maximum": 1,
          "description": "cubesigner-signer is singleton-only to avoid duplicate signing requests for the same signer identity."
        },
        "strategy": {
          "const": "RollingUpdate",
          "description": "cubesigner-signer uses the chart-owned RollingUpdate strategy."
        }
      }
    }
  }
}
